import React from "react";
import { Link } from "react-router-dom";

const Top = () => {
    return (
        <>
            <button>新規登録</button>
            <button>ログイン</button>
        </>
    );
};

export default Top;